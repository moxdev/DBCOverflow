<div class="container">
<%= form_for @question, remote: true do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>

  <%= f.label :content %>
  <%= f.text_area :content %><br>

  <%= f.submit %>
<% end %>

</div>

